service refnotify_td /vendor/bin/refnotify -t 0
    class core
    disabled
    user system
    group system radio shell

service refnotify_w /vendor/bin/refnotify -t 1
    class core
    disabled
    user system
    group system radio shell

service refnotify_l /vendor/bin/refnotify -t 2
    class core
    disabled
    user system
    group system radio shell
